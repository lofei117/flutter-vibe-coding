name: flutter_vibe_app
description: Flutter vibe coding app with UME and a local AI server.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_vibe_ume:
    path: ../packages/flutter_vibe_ume

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
