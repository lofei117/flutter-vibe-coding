name: mobile_vibe_demo
description: Minimal mobile vibe coding demo with UME and a local AI server.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.3.2
  ume_core: ^2.0.2

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
